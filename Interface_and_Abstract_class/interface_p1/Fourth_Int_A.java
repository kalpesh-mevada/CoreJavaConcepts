package interface_p1;
/*	By default, Every variable in interface is 
	final & static
	-	we can never create object of interface.
	-	we can never keep main method in interface*/
public interface Fourth_Int_A {
	// by default public
	int i = 10;// by default final & static
}
